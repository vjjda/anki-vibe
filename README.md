# Anki Vibe

A powerful middleware to manage Anki profiles and decks via Code-as-Source-of-Truth.

## Setup

1. Install dependencies:
   ```bash
   poetry install
   ```

2. Run CLI:
   ```bash
   poetry run python src/main.py --help
   ```